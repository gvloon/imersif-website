{
  "routes": [
    {
      "method": "GET",
      "path": "/peripherals",
      "handler": "peripheral.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/peripherals/:slug",
      "handler": "peripheral.findBySlug",
      "config": {
        "policies": []
      }
    }
  ]
}
