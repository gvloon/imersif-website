{
  "routes": [
    {
      "method": "GET",
      "path": "/patterns",
      "handler": "pattern.find"
    },
    {
      "method": "GET",
      "path": "/patterns/:slug",
      "handler": "pattern.findBySlug"
    }
  ]
}
