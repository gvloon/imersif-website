{
  "routes": [
    {
      "method": "GET",
      "path": "/pattern-categories",
      "handler": "pattern-category.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/pattern-categories/:slug",
      "handler": "pattern-category.findBySlug",
      "config": {
        "policies": []
      }
    }
  ]
}
