{
  "routes": [
    {
      "method": "GET",
      "path": "/device-types",
      "handler": "device-type.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/device-types/:slug",
      "handler": "device-type.findBySlug",
      "config": {
        "policies": []
      }
    }
  ]
}
