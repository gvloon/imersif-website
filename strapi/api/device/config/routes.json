{
  "routes": [
    {
      "method": "GET",
      "path": "/devices",
      "handler": "device.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/devices/:slug",
      "handler": "device.findBySlug",
      "config": {
        "policies": []
      }
    }
  ]
}
