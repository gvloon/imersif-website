{
  "routes": [
    {
      "method": "GET",
      "path": "/peripheral-types",
      "handler": "peripheral-type.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/peripheral-types/:slug",
      "handler": "peripheral-type.findBySlug",
      "config": {
        "policies": []
      }
    }
  ]
}
